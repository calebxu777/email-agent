[project]
name = "new-project"
version = "0.1.0"
description = "A security-first email support agent prototype."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
llm = [
    "langchain-core>=0.3,<0.4",
]
